import { AverageParkRatingPipe } from './average-park-rating.pipe';

describe('AverageParkRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new AverageParkRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
